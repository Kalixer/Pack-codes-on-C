#include <stdio.h>
#include <stdlib.h>

int main()
{
    int limit = 5;
    int a = 1;
    char letra = '#';

    while(a<=limit)
    {
        printf("\n %c", letra);
        a++;
    }

    return 0;
}
